const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();

const exercisesByLesson = {
  // ============ CHAPTER 25: Intro to ML (fill gaps) ============
  'ml-fundamentals': [
    { title: 'What is Machine Learning?', prompt: 'Define machine learning in your own words. How is it different from traditional programming?', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Learning from data', 'No explicit programming'], solution: 'Machine learning is a field where computers learn patterns from data instead of being explicitly programmed with rules. Traditional programming: input + rules → output. ML: input + output → rules (learned).' },
    { title: 'Types of ML', prompt: 'Explain the difference between supervised, unsupervised, and reinforcement learning with examples.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 10, hints: ['Labeled vs unlabeled', 'Rewards'], solution: 'Supervised: Learn from labeled data (spam detection, price prediction)\nUnsupervised: Find patterns in unlabeled data (clustering customers, anomaly detection)\nReinforcement: Learn from rewards/penalties (game AI, robotics)' },
    { title: 'Features and Labels', prompt: 'What are features and labels in ML? Give an example for predicting house prices.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Input vs output', 'X and y'], solution: 'Features (X): Input variables used to make predictions (square footage, bedrooms, location, age)\nLabels (y): Target variable to predict (house price)\n\nFeatures describe the data, labels are what we want to predict.' },
    { title: 'Classification vs Regression', prompt: 'Compare classification and regression tasks with examples.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 10, hints: ['Discrete vs continuous', 'Categories vs numbers'], solution: 'Classification: Predict categories/classes\n- Email: spam or not spam\n- Image: cat, dog, or bird\n\nRegression: Predict continuous values\n- House price: $350,000\n- Temperature: 72.5°F\n- Stock price: $150.25' },
    { title: 'Overfitting Concept', prompt: 'Explain overfitting using an analogy. How can you detect it?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Memorization vs learning', 'Train vs test performance'], solution: 'Analogy: A student who memorizes answers instead of understanding concepts - they ace practice tests but fail new questions.\n\nDetection:\n- High training accuracy, low test accuracy\n- Model is too complex for the data\n- Performance gap between train/test increases' },
    { title: 'Simple Linear Model', prompt: 'Implement a simple linear model y = wx + b and make predictions.', type: 'CODE', difficulty: 'BEGINNER', xpReward: 15, starterCode: 'class LinearModel:\n    def __init__(self, w=1.0, b=0.0):\n        self.w = w\n        self.b = b\n    \n    def predict(self, x):\n        # y = wx + b\n        return self.w * x + self.b\n\n# Create model with w=2, b=1\nmodel = LinearModel(w=2, b=1)\n\n# Predict for x = 0, 1, 2, 3\nfor x in [0, 1, 2, 3]:\n    y = model.predict(x)\n    print(f"x={x} -> y={y}")', hints: ['y = wx + b', 'Linear relationship'], solution: '# x=0 -> y=1, x=1 -> y=3, x=2 -> y=5, x=3 -> y=7' },
    { title: 'Training Data Example', prompt: 'Create a training dataset for predicting if a student will pass based on study hours and attendance.', type: 'CODE', difficulty: 'BEGINNER', xpReward: 10, starterCode: '# Training data: [study_hours, attendance_pct] -> pass (1) or fail (0)\nX_train = [\n    [5, 90],   # 5 hours study, 90% attendance\n    [2, 60],   # 2 hours study, 60% attendance\n    [8, 95],\n    [1, 40],\n    [6, 85],\n    [3, 50],\n]\n\ny_train = [1, 0, 1, 0, 1, 0]  # 1=pass, 0=fail\n\n# Print the data\nprint("Study Hours | Attendance | Pass?")\nfor features, label in zip(X_train, y_train):\n    print(f"    {features[0]}      |     {features[1]}%    |  {\"Yes\" if label else \"No\"}")', hints: ['Features: study hours, attendance', 'Label: pass/fail'], solution: '# Shows relationship between features and outcomes' },
    { title: 'Mean Squared Error', prompt: 'Implement the Mean Squared Error loss function.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 15, starterCode: 'def mse(y_true, y_pred):\n    """Calculate Mean Squared Error."""\n    n = len(y_true)\n    total_error = 0\n    for true, pred in zip(y_true, y_pred):\n        total_error += (true - pred) ** 2\n    return total_error / n\n\n# Test\ny_true = [3, 5, 2, 7]\ny_pred = [2.5, 5, 2.1, 8]\n\nerror = mse(y_true, y_pred)\nprint(f"MSE: {error:.4f}")', hints: ['Square the differences', 'Average them'], solution: '# MSE = ((3-2.5)² + (5-5)² + (2-2.1)² + (7-8)²) / 4 = 0.315' },
    { title: 'Accuracy Metric', prompt: 'Implement accuracy for classification: correct predictions / total predictions.', type: 'CODE', difficulty: 'BEGINNER', xpReward: 10, starterCode: 'def accuracy(y_true, y_pred):\n    """Calculate classification accuracy."""\n    correct = sum(t == p for t, p in zip(y_true, y_pred))\n    return correct / len(y_true)\n\n# Test\ny_true = [1, 0, 1, 1, 0, 1, 0, 0]\ny_pred = [1, 0, 0, 1, 0, 1, 1, 0]\n\nacc = accuracy(y_true, y_pred)\nprint(f"Accuracy: {acc:.2%}")', hints: ['Count correct predictions', 'Divide by total'], solution: '# 6 correct out of 8 = 75%' },
    { title: 'Train-Test Split Concept', prompt: 'Why do we split data into training and testing sets? What happens if we dont?', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 10, hints: ['Evaluation fairness', 'Detecting overfitting'], solution: 'We split to evaluate on unseen data:\n- Training set: model learns patterns\n- Test set: evaluate generalization\n\nWithout split:\n- Model might memorize training data\n- No way to detect overfitting\n- Overly optimistic performance estimates\n- Model may fail on new data' },
  ],

  'supervised-learning-basics': [
    { title: 'Supervised Learning Definition', prompt: 'Define supervised learning and explain why it is called "supervised".', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Teacher analogy', 'Labeled data'], solution: 'Supervised learning uses labeled data where we know the correct answers. Called "supervised" because it\'s like having a teacher (the labels) who tells the model the right answer during training, allowing it to learn from its mistakes.' },
    { title: 'Common Algorithms', prompt: 'List 5 common supervised learning algorithms.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Classification and regression', 'Trees, linear models'], solution: '1. Linear Regression (regression)\n2. Logistic Regression (classification)\n3. Decision Trees (both)\n4. Random Forest (both)\n5. Support Vector Machines (both)\n6. K-Nearest Neighbors (both)\n7. Neural Networks (both)' },
    { title: 'Simple Prediction Rule', prompt: 'Create a simple rule-based classifier for spam detection using keyword counts.', type: 'CODE', difficulty: 'BEGINNER', xpReward: 15, starterCode: 'def is_spam(email_text):\n    """Simple spam detector based on keywords."""\n    spam_words = ["free", "winner", "click", "urgent", "act now"]\n    email_lower = email_text.lower()\n    \n    spam_count = sum(1 for word in spam_words if word in email_lower)\n    \n    # If 2+ spam words, classify as spam\n    return spam_count >= 2\n\n# Test\nemails = [\n    "Meeting tomorrow at 3pm",\n    "FREE WINNER! Click now for your prize!",\n    "Project deadline is urgent",\n    "Act now! Free gift inside! Click here!"\n]\n\nfor email in emails:\n    result = "SPAM" if is_spam(email) else "NOT SPAM"\n    print(f"{result}: {email[:40]}...")', hints: ['Count spam keywords', 'Threshold decision'], solution: '# Detects spam based on keyword count' },
    { title: 'Decision Boundary', prompt: 'Explain what a decision boundary is in classification.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['Separating classes', 'Line or surface'], solution: 'A decision boundary is the line/surface that separates different classes in feature space.\n\nLinear models create straight lines/planes.\nNon-linear models (trees, neural nets) can create curved boundaries.\n\nPoints on one side → class A\nPoints on other side → class B' },
    { title: 'Implement Simple Classifier', prompt: 'Implement a classifier that predicts pass/fail based on a score threshold.', type: 'CODE', difficulty: 'BEGINNER', xpReward: 10, starterCode: 'def predict_pass_fail(scores, threshold=60):\n    """Predict pass (1) or fail (0) based on threshold."""\n    predictions = []\n    for score in scores:\n        if score >= threshold:\n            predictions.append(1)  # Pass\n        else:\n            predictions.append(0)  # Fail\n    return predictions\n\n# Test\nscores = [45, 62, 78, 55, 90, 38, 61]\npredictions = predict_pass_fail(scores)\n\nfor score, pred in zip(scores, predictions):\n    result = "Pass" if pred else "Fail"\n    print(f"Score {score}: {result}")', hints: ['Compare to threshold', 'Return 1 or 0'], solution: '# Scores >= 60 pass, others fail' },
    { title: 'Learning from Errors', prompt: 'Explain how supervised learning algorithms "learn" from their mistakes.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Loss function', 'Gradient descent'], solution: 'Learning process:\n1. Make prediction on training data\n2. Compare to true label (compute loss/error)\n3. Adjust model parameters to reduce error\n4. Repeat many times\n\nGradient descent finds which direction to adjust parameters to minimize loss. Like adjusting a dial until you get the right answer.' },
    { title: 'Feature Importance', prompt: 'Why might some features be more important than others for prediction?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['Predictive power', 'Correlation with target'], solution: 'Some features have stronger relationships with the target:\n\nFor house prices:\n- Square footage: very important (strong correlation)\n- House color: not important (no correlation)\n\nImportant features:\n- Have predictive power\n- Correlate with target\n- Add information not captured by other features' },
    { title: 'Confusion Matrix Concept', prompt: 'What is a confusion matrix? Draw one for a binary classifier.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['TP, TN, FP, FN', '2x2 matrix'], solution: 'Confusion matrix shows prediction breakdown:\n\n                Predicted\n              Pos    Neg\nActual Pos [  TP  |  FN  ]\nActual Neg [  FP  |  TN  ]\n\nTP: True Positive (correct positive)\nTN: True Negative (correct negative)\nFP: False Positive (wrong positive)\nFN: False Negative (wrong negative)' },
    { title: 'Implement Confusion Matrix', prompt: 'Calculate confusion matrix values from predictions.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: 'def confusion_matrix(y_true, y_pred):\n    """Calculate TP, TN, FP, FN."""\n    tp = tn = fp = fn = 0\n    \n    for true, pred in zip(y_true, y_pred):\n        if true == 1 and pred == 1:\n            tp += 1\n        elif true == 0 and pred == 0:\n            tn += 1\n        elif true == 0 and pred == 1:\n            fp += 1\n        else:  # true == 1 and pred == 0\n            fn += 1\n    \n    return {"TP": tp, "TN": tn, "FP": fp, "FN": fn}\n\n# Test\ny_true = [1, 1, 0, 1, 0, 0, 1, 0]\ny_pred = [1, 0, 0, 1, 1, 0, 1, 0]\n\ncm = confusion_matrix(y_true, y_pred)\nprint(f"Confusion Matrix: {cm}")\nprint(f"Accuracy: {(cm[\"TP\"] + cm[\"TN\"]) / len(y_true):.2%}")', hints: ['Check all 4 combinations', 'Count each type'], solution: '# TP=3, TN=3, FP=1, FN=1' },
    { title: 'Precision and Recall', prompt: 'Implement precision and recall calculations.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: 'def precision_recall(y_true, y_pred):\n    """Calculate precision and recall."""\n    tp = sum(1 for t, p in zip(y_true, y_pred) if t == 1 and p == 1)\n    fp = sum(1 for t, p in zip(y_true, y_pred) if t == 0 and p == 1)\n    fn = sum(1 for t, p in zip(y_true, y_pred) if t == 1 and p == 0)\n    \n    precision = tp / (tp + fp) if (tp + fp) > 0 else 0\n    recall = tp / (tp + fn) if (tp + fn) > 0 else 0\n    \n    return precision, recall\n\ny_true = [1, 1, 1, 1, 0, 0, 0, 0]\ny_pred = [1, 1, 0, 0, 1, 0, 0, 0]\n\nprec, rec = precision_recall(y_true, y_pred)\nprint(f"Precision: {prec:.2%}")  # Of predicted positives, how many correct?\nprint(f"Recall: {rec:.2%}")      # Of actual positives, how many found?', hints: ['Precision = TP / (TP + FP)', 'Recall = TP / (TP + FN)'], solution: '# Precision: 66.67%, Recall: 50%' },
  ],

  // ============ CHAPTER 27 (expand existing) ============
  'neural-network-architecture': [
    { title: 'Neuron Anatomy', prompt: 'Draw and describe the components of an artificial neuron.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Inputs, weights, sum, activation'], solution: 'Artificial neuron:\n1. Inputs (x1, x2, ...) - data coming in\n2. Weights (w1, w2, ...) - importance of each input\n3. Bias (b) - threshold adjustment\n4. Weighted sum: z = Σ(wi × xi) + b\n5. Activation function: a = f(z)\n6. Output (a) - sent to next layer' },
    { title: 'Why Layers?', prompt: 'Why do neural networks have multiple layers? What does each layer learn?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Abstraction levels', 'Simple to complex'], solution: 'Multiple layers learn hierarchical representations:\n\nImage example:\n- Layer 1: edges, colors\n- Layer 2: textures, corners\n- Layer 3: parts (eyes, wheels)\n- Layer 4: objects (faces, cars)\n\nEach layer builds on previous, learning increasingly abstract features.' },
    { title: 'Network Width vs Depth', prompt: 'Compare adding more neurons per layer vs adding more layers.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Capacity vs abstraction'], solution: 'Width (more neurons):\n- More features at same abstraction level\n- Can memorize more\n- May overfit\n\nDepth (more layers):\n- More abstraction levels\n- Hierarchical learning\n- Harder to train (vanishing gradients)\n\nBoth increase model capacity, but in different ways.' },
    { title: 'Implement Neuron', prompt: 'Implement a single neuron with multiple inputs.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: 'import math\n\ndef sigmoid(x):\n    return 1 / (1 + math.exp(-max(-500, min(500, x))))\n\nclass Neuron:\n    def __init__(self, n_inputs):\n        import random\n        self.weights = [random.uniform(-1, 1) for _ in range(n_inputs)]\n        self.bias = random.uniform(-1, 1)\n    \n    def forward(self, inputs):\n        # Calculate weighted sum + bias\n        z = sum(w * x for w, x in zip(self.weights, inputs)) + self.bias\n        # Apply activation\n        return sigmoid(z)\n\n# Create neuron with 3 inputs\nneuron = Neuron(3)\n\n# Test with sample inputs\ninputs = [0.5, 0.3, 0.8]\noutput = neuron.forward(inputs)\nprint(f"Inputs: {inputs}")\nprint(f"Weights: {[round(w, 3) for w in neuron.weights]}")\nprint(f"Bias: {round(neuron.bias, 3)}")\nprint(f"Output: {round(output, 3)}")', hints: ['Weighted sum', 'Apply activation'], solution: '# Output between 0 and 1' },
    { title: 'Layer Class', prompt: 'Implement a Layer class containing multiple neurons.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 25, starterCode: 'import math\nimport random\n\ndef sigmoid(x):\n    return 1 / (1 + math.exp(-max(-500, min(500, x))))\n\nclass Layer:\n    def __init__(self, n_inputs, n_neurons):\n        # Each neuron has weights for all inputs\n        self.weights = [[random.uniform(-1, 1) for _ in range(n_neurons)] \n                        for _ in range(n_inputs)]\n        self.biases = [random.uniform(-1, 1) for _ in range(n_neurons)]\n    \n    def forward(self, inputs):\n        outputs = []\n        for j in range(len(self.biases)):\n            z = sum(inputs[i] * self.weights[i][j] for i in range(len(inputs)))\n            z += self.biases[j]\n            outputs.append(sigmoid(z))\n        return outputs\n\n# Test\nlayer = Layer(n_inputs=3, n_neurons=4)\ninputs = [0.5, 0.3, 0.8]\noutputs = layer.forward(inputs)\nprint(f"Input size: {len(inputs)}")\nprint(f"Output size: {len(outputs)}")\nprint(f"Outputs: {[round(o, 3) for o in outputs]}")', hints: ['Matrix of weights', 'One output per neuron'], solution: '# 3 inputs -> 4 outputs' },
    { title: 'Connect Multiple Layers', prompt: 'Build a network by connecting multiple layers.', type: 'CODE', difficulty: 'ADVANCED', xpReward: 30, starterCode: 'import math\nimport random\n\ndef sigmoid(x):\n    return 1 / (1 + math.exp(-max(-500, min(500, x))))\n\nclass Layer:\n    def __init__(self, n_inputs, n_neurons):\n        self.weights = [[random.uniform(-1, 1) for _ in range(n_neurons)] \n                        for _ in range(n_inputs)]\n        self.biases = [random.uniform(-1, 1) for _ in range(n_neurons)]\n    \n    def forward(self, inputs):\n        return [sigmoid(sum(inputs[i] * self.weights[i][j] for i in range(len(inputs))) + self.biases[j])\n                for j in range(len(self.biases))]\n\nclass Network:\n    def __init__(self, layer_sizes):\n        self.layers = []\n        for i in range(len(layer_sizes) - 1):\n            self.layers.append(Layer(layer_sizes[i], layer_sizes[i+1]))\n    \n    def forward(self, inputs):\n        for layer in self.layers:\n            inputs = layer.forward(inputs)\n        return inputs\n\n# Create network: 2 inputs -> 4 hidden -> 3 hidden -> 1 output\nnet = Network([2, 4, 3, 1])\noutput = net.forward([0.5, 0.8])\nprint(f"Network output: {output[0]:.4f}")', hints: ['Pass output of one layer to next', 'Chain forward passes'], solution: '# Multi-layer forward pass' },
  ],

  'forward-propagation': [
    { title: 'Forward Pass Definition', prompt: 'What is forward propagation? Describe each step.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Input to output', 'Layer by layer'], solution: 'Forward propagation passes data through the network:\n\n1. Input layer receives data\n2. For each hidden layer:\n   - Compute weighted sum (z = Wx + b)\n   - Apply activation function (a = f(z))\n   - Pass output to next layer\n3. Output layer produces prediction\n\nData flows forward, no going back.' },
    { title: 'Matrix Multiplication View', prompt: 'Explain how forward propagation can be viewed as matrix multiplication.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Batch processing', 'Efficient computation'], solution: 'For layer with inputs X (batch × features):\n\nZ = X @ W + b  (matrix multiplication)\nA = activation(Z)\n\nThis processes entire batches at once:\n- X: (batch_size × input_features)\n- W: (input_features × output_neurons)\n- Z: (batch_size × output_neurons)\n\nGPUs excel at matrix operations, making this fast.' },
    { title: 'Trace Forward Pass', prompt: 'Manually trace a forward pass through a tiny network.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: '# Tiny network: 2 inputs -> 2 hidden -> 1 output\n# Using step activation (1 if z >= 0, else 0)\n\ndef step(x): return 1 if x >= 0 else 0\n\n# Weights and biases (fixed for tracing)\nW1 = [[0.5, -0.5], [0.5, 0.5]]  # 2x2\nb1 = [-0.5, -0.5]\nW2 = [[1], [1]]  # 2x1\nb2 = [-1]\n\n# Input\nx = [1, 1]\n\n# Layer 1\nprint("Layer 1:")\nz1 = [sum(x[i] * W1[i][j] for i in range(2)) + b1[j] for j in range(2)]\nprint(f"  z1 = {z1}")\na1 = [step(z) for z in z1]\nprint(f"  a1 = {a1}")\n\n# Layer 2\nprint("Layer 2:")\nz2 = [sum(a1[i] * W2[i][j] for i in range(2)) + b2[j] for j in range(1)]\nprint(f"  z2 = {z2}")\na2 = [step(z) for z in z2]\nprint(f"  output = {a2}")', hints: ['Calculate z then a for each layer', 'Pass a to next layer'], solution: '# Traces computation step by step' },
    { title: 'Activation After Each Layer', prompt: 'Why do we apply activation after each layer, not just the output?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Linearity problem', 'Composition of functions'], solution: 'Without activations between layers:\n- Each layer is linear: y = Wx + b\n- Stacking linear layers: still linear!\n- y = W2(W1x + b1) + b2 = W2W1x + W2b1 + b2 = W\'x + b\'\n\nActivations add non-linearity, allowing the network to learn complex patterns that arent just straight lines.' },
    { title: 'Implement Batch Forward', prompt: 'Implement forward pass that handles a batch of inputs.', type: 'CODE', difficulty: 'ADVANCED', xpReward: 25, starterCode: 'import math\n\ndef sigmoid(x): return 1 / (1 + math.exp(-max(-500, min(500, x))))\n\ndef forward_batch(X_batch, weights, biases):\n    """\n    X_batch: list of input vectors\n    Returns: list of output vectors\n    """\n    outputs = []\n    for x in X_batch:\n        current = x\n        for W, b in zip(weights, biases):\n            # Process one layer\n            next_layer = []\n            for j in range(len(b)):\n                z = sum(current[i] * W[i][j] for i in range(len(current))) + b[j]\n                next_layer.append(sigmoid(z))\n            current = next_layer\n        outputs.append(current)\n    return outputs\n\n# Test with batch of 3 samples\nX_batch = [[0.1, 0.2], [0.5, 0.5], [0.9, 0.1]]\nW = [[[0.5, 0.5], [0.5, -0.5]]]  # 1 layer: 2->2\nb = [[0, 0]]\n\nresults = forward_batch(X_batch, W, b)\nfor x, y in zip(X_batch, results):\n    print(f"{x} -> {[round(v, 3) for v in y]}")', hints: ['Loop over batch', 'Same computation for each'], solution: '# Processes multiple samples' },
  ],

  // ============ CHAPTER 30: PyTorch Fundamentals (expand) ============
  'pytorch-tensors': [
    { title: 'What is a Tensor?', prompt: 'Explain what a tensor is and how it relates to scalars, vectors, and matrices.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Generalization', 'Dimensions'], solution: 'Tensor is a generalization of arrays to any number of dimensions:\n- 0D tensor: scalar (single number)\n- 1D tensor: vector [1, 2, 3]\n- 2D tensor: matrix [[1,2], [3,4]]\n- 3D tensor: cube of numbers (e.g., RGB image)\n- nD tensor: n-dimensional array\n\nTensors are the fundamental data structure in PyTorch.' },
    { title: 'Tensor Operations', prompt: 'List 5 common tensor operations in deep learning.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Math operations', 'Shape operations'], solution: '1. Addition/Subtraction (element-wise)\n2. Matrix multiplication (@ or matmul)\n3. Reshape/View (change dimensions)\n4. Broadcasting (align different shapes)\n5. Reduction (sum, mean, max along axes)\n6. Concatenation (join tensors)\n7. Indexing/Slicing (select parts)' },
    { title: 'Implement Tensor Class (Basic)', prompt: 'Implement a simple Tensor class with basic operations.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 25, starterCode: 'class SimpleTensor:\n    def __init__(self, data):\n        self.data = data\n        self.shape = self._get_shape(data)\n    \n    def _get_shape(self, data):\n        if not isinstance(data, list):\n            return ()\n        if not data:\n            return (0,)\n        if not isinstance(data[0], list):\n            return (len(data),)\n        return (len(data),) + self._get_shape(data[0])\n    \n    def __add__(self, other):\n        # Element-wise addition (assumes same shape)\n        if isinstance(other, SimpleTensor):\n            other = other.data\n        return SimpleTensor(self._add_recursive(self.data, other))\n    \n    def _add_recursive(self, a, b):\n        if not isinstance(a, list):\n            return a + b\n        return [self._add_recursive(ai, bi) for ai, bi in zip(a, b)]\n    \n    def __repr__(self):\n        return f"SimpleTensor({self.data})"\n\n# Test\nt1 = SimpleTensor([[1, 2], [3, 4]])\nt2 = SimpleTensor([[5, 6], [7, 8]])\nprint(f"t1 shape: {t1.shape}")\nprint(f"t1 + t2 = {t1 + t2}")', hints: ['Recursive for nested lists', 'Store shape'], solution: '# Basic tensor with addition' },
    { title: 'GPU Acceleration', prompt: 'Explain why GPUs are important for deep learning and how tensors use them.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Parallel processing', 'CUDA'], solution: 'GPUs have thousands of cores for parallel operations:\n\nCPU: 8-16 powerful cores (sequential)\nGPU: 4000+ simple cores (parallel)\n\nTensor operations (matrix multiply, convolution) are highly parallelizable.\n\nIn PyTorch:\n- tensor.to("cuda") moves to GPU\n- tensor.to("cpu") moves to CPU\n- All operations on same device' },
    { title: 'Autograd Concept', prompt: 'What is autograd and why is it essential for training neural networks?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Automatic differentiation', 'Backpropagation'], solution: 'Autograd automatically computes gradients:\n\n1. Forward pass builds computation graph\n2. backward() traverses graph in reverse\n3. Computes gradient of loss w.r.t. each parameter\n\nEssential because:\n- Manual gradient calculation is error-prone\n- Complex models have millions of parameters\n- Enables easy experimentation with architectures' },
    { title: 'Implement Simple Autograd', prompt: 'Implement a Value class with basic autograd (addition and multiplication).', type: 'CODE', difficulty: 'ADVANCED', xpReward: 35, starterCode: 'class Value:\n    def __init__(self, data, _children=(), _op=\'\'):\n        self.data = data\n        self.grad = 0.0\n        self._backward = lambda: None\n        self._prev = set(_children)\n        self._op = _op\n    \n    def __add__(self, other):\n        other = other if isinstance(other, Value) else Value(other)\n        out = Value(self.data + other.data, (self, other), \'+\')\n        \n        def _backward():\n            self.grad += out.grad\n            other.grad += out.grad\n        out._backward = _backward\n        return out\n    \n    def __mul__(self, other):\n        other = other if isinstance(other, Value) else Value(other)\n        out = Value(self.data * other.data, (self, other), \'*\')\n        \n        def _backward():\n            self.grad += other.data * out.grad\n            other.grad += self.data * out.grad\n        out._backward = _backward\n        return out\n    \n    def backward(self):\n        # Topological sort\n        topo = []\n        visited = set()\n        def build_topo(v):\n            if v not in visited:\n                visited.add(v)\n                for child in v._prev:\n                    build_topo(child)\n                topo.append(v)\n        build_topo(self)\n        \n        self.grad = 1.0\n        for node in reversed(topo):\n            node._backward()\n    \n    def __repr__(self):\n        return f"Value(data={self.data:.4f}, grad={self.grad:.4f})"\n\n# Test\na = Value(2.0)\nb = Value(3.0)\nc = a * b  # 6\nd = c + a  # 8\nd.backward()\n\nprint(f"a = {a}")\nprint(f"b = {b}")\nprint(f"c = a*b = {c}")\nprint(f"d = c+a = {d}")', hints: ['Track operations', 'Implement backward for each op'], solution: '# Gradients: d/da = b + 1 = 4, d/db = a = 2' },
    { title: 'Tensor Shapes in NNs', prompt: 'Explain common tensor shapes in neural networks: batch, channels, height, width.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['NCHW format', 'Batch dimension'], solution: 'Common tensor shapes:\n\nFully Connected: (batch_size, features)\n- Example: (32, 784) - 32 flattened images\n\nImage (NCHW): (batch, channels, height, width)\n- Example: (32, 3, 224, 224) - 32 RGB images\n\nSequence (NLP): (batch, sequence_length, hidden_size)\n- Example: (16, 100, 512) - 16 sentences\n\nBatch dimension allows parallel processing.' },
    { title: 'Broadcasting Rules', prompt: 'Explain tensor broadcasting with an example.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 15, hints: ['Implicit expansion', 'Dimension alignment'], solution: 'Broadcasting automatically expands tensors for operations:\n\nRules:\n1. Align shapes from the right\n2. Dimensions must match or be 1\n3. Size-1 dimensions are broadcast (copied)\n\nExample:\n[3, 4] + [[1], [2]] (shape 2,1)\n\n[[1, 2, 3, 4],    [[1, 1, 1, 1],    [[2, 3, 4, 5],\n [5, 6, 7, 8]]  +  [2, 2, 2, 2]] =   [7, 8, 9, 10]]' },
  ],

  'pytorch-nn-module': [
    { title: 'nn.Module Purpose', prompt: 'What is nn.Module and why is it the base class for all neural network components?', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 10, hints: ['Parameter tracking', 'Standard interface'], solution: 'nn.Module provides:\n\n1. Parameter registration (tracks learnable weights)\n2. Forward method definition (how data flows)\n3. Training/eval mode switching\n4. GPU/CPU device management\n5. State dict for saving/loading\n6. Nested module support\n\nAll layers inherit from it, enabling consistent API.' },
    { title: 'Implement Simple Module', prompt: 'Implement a basic Module class that tracks parameters.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 25, starterCode: 'class Parameter:\n    def __init__(self, data):\n        self.data = data\n        self.grad = None\n\nclass Module:\n    def __init__(self):\n        self._parameters = {}\n        self._modules = {}\n    \n    def register_parameter(self, name, param):\n        self._parameters[name] = param\n    \n    def parameters(self):\n        """Return all parameters."""\n        params = list(self._parameters.values())\n        for module in self._modules.values():\n            params.extend(module.parameters())\n        return params\n    \n    def __call__(self, *args):\n        return self.forward(*args)\n\nclass Linear(Module):\n    def __init__(self, in_features, out_features):\n        super().__init__()\n        import random\n        self.register_parameter(\'weight\', Parameter(\n            [[random.gauss(0, 0.1) for _ in range(out_features)] for _ in range(in_features)]))\n        self.register_parameter(\'bias\', Parameter([0.0] * out_features))\n    \n    def forward(self, x):\n        # y = x @ W + b (simplified for single sample)\n        W = self._parameters[\'weight\'].data\n        b = self._parameters[\'bias\'].data\n        return [sum(x[i] * W[i][j] for i in range(len(x))) + b[j] for j in range(len(b))]\n\n# Test\nlinear = Linear(3, 2)\nprint(f"Parameters: {len(linear.parameters())}")\nprint(f"Output: {linear([1.0, 2.0, 3.0])")', hints: ['Store parameters in dict', 'Forward defines computation'], solution: '# Linear layer with tracked parameters' },
    { title: 'Building Network from Modules', prompt: 'Explain how to compose multiple modules into a network.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['Sequential container', 'Nested modules'], solution: 'Modules compose hierarchically:\n\n```\nclass MyNetwork(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.layer1 = nn.Linear(784, 256)\n        self.layer2 = nn.Linear(256, 10)\n    \n    def forward(self, x):\n        x = F.relu(self.layer1(x))\n        return self.layer2(x)\n```\n\nOr use nn.Sequential for simple stacks:\n```\nnet = nn.Sequential(\n    nn.Linear(784, 256),\n    nn.ReLU(),\n    nn.Linear(256, 10)\n)\n```' },
    { title: 'Training vs Eval Mode', prompt: 'What is the difference between training and evaluation mode in PyTorch?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['Dropout behavior', 'BatchNorm behavior'], solution: 'model.train() vs model.eval():\n\nDropout:\n- Train: randomly drops neurons\n- Eval: uses all neurons (scaled)\n\nBatchNorm:\n- Train: uses batch statistics\n- Eval: uses running statistics\n\nAlways set correct mode:\n- model.train() before training loop\n- model.eval() before inference/testing\n- with torch.no_grad(): for inference efficiency' },
    { title: 'Saving and Loading', prompt: 'How do you save and load a model in PyTorch?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['state_dict', 'save/load methods'], solution: 'Save model:\n```\ntorch.save(model.state_dict(), "model.pth")\n```\n\nLoad model:\n```\nmodel = MyModel()  # Create same architecture\nmodel.load_state_dict(torch.load("model.pth"))\nmodel.eval()\n```\n\nstate_dict contains all learnable parameters.\nYou can also save the entire model:\n```\ntorch.save(model, "model.pth")\nmodel = torch.load("model.pth")\n```' },
    { title: 'Implement Sequential', prompt: 'Implement a simple Sequential container.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: 'class Sequential:\n    def __init__(self, *layers):\n        self.layers = layers\n    \n    def __call__(self, x):\n        for layer in self.layers:\n            x = layer(x)\n        return x\n    \n    def parameters(self):\n        params = []\n        for layer in self.layers:\n            if hasattr(layer, \'parameters\'):\n                params.extend(layer.parameters())\n        return params\n\nclass ReLU:\n    def __call__(self, x):\n        return [max(0, v) for v in x]\n    def parameters(self):\n        return []\n\n# Use the Linear class from before\nimport random\nclass Linear:\n    def __init__(self, in_f, out_f):\n        self.W = [[random.gauss(0, 0.1) for _ in range(out_f)] for _ in range(in_f)]\n        self.b = [0.0] * out_f\n    def __call__(self, x):\n        return [sum(x[i]*self.W[i][j] for i in range(len(x))) + self.b[j] for j in range(len(self.b))]\n    def parameters(self):\n        return [self.W, self.b]\n\n# Build network\nnet = Sequential(\n    Linear(2, 4),\n    ReLU(),\n    Linear(4, 1)\n)\n\nprint(f"Output: {net([0.5, 0.5])}")', hints: ['Chain layer calls', 'Collect all parameters'], solution: '# Sequential container for stacking layers' },
  ],

  'pytorch-training-loop': [
    { title: 'Training Loop Steps', prompt: 'List the 5 essential steps in a PyTorch training loop.', type: 'TEXT_RESPONSE', difficulty: 'BEGINNER', xpReward: 5, hints: ['Forward, loss, backward, step, zero'], solution: '1. Forward pass: outputs = model(inputs)\n2. Compute loss: loss = criterion(outputs, targets)\n3. Backward pass: loss.backward()\n4. Update weights: optimizer.step()\n5. Zero gradients: optimizer.zero_grad()\n\nRepeat for each batch, for multiple epochs.' },
    { title: 'Why Zero Gradients?', prompt: 'Why must we call optimizer.zero_grad() and when should we call it?', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['Gradient accumulation', 'Default behavior'], solution: 'PyTorch accumulates gradients by default (adds to existing).\n\nWithout zero_grad():\n- Gradients from previous batches add up\n- Wrong updates, training fails\n\nWhen to call:\n- Before each batch (most common)\n- After optimizer.step()\n- Or before loss.backward()\n\nGradient accumulation is useful for simulating larger batches.' },
    { title: 'Implement Training Loop', prompt: 'Implement a basic training loop (pseudo-code style).', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 25, starterCode: 'import random\nimport math\n\n# Simple implementations\ndef sigmoid(x): return 1 / (1 + math.exp(-max(-500, min(500, x))))\ndef mse_loss(pred, target): return (pred - target) ** 2\n\nclass SimpleNet:\n    def __init__(self):\n        self.w = random.gauss(0, 0.1)\n        self.b = 0.0\n    \n    def forward(self, x):\n        return sigmoid(self.w * x + self.b)\n    \n    def parameters(self):\n        return [(\'w\', self.w), (\'b\', self.b)]\n\n# Training data: XOR-like (simplified to 1D)\nX = [0.0, 1.0]\nY = [0.0, 1.0]\n\nmodel = SimpleNet()\nlearning_rate = 0.5\n\nfor epoch in range(100):\n    total_loss = 0\n    for x, y in zip(X, Y):\n        # Forward\n        pred = model.forward(x)\n        loss = mse_loss(pred, y)\n        total_loss += loss\n        \n        # Backward (manual gradient)\n        # d_loss/d_pred = 2 * (pred - y)\n        # d_pred/d_w = pred * (1 - pred) * x (sigmoid derivative)\n        d_loss = 2 * (pred - y)\n        d_pred_w = pred * (1 - pred) * x\n        d_pred_b = pred * (1 - pred)\n        \n        # Update\n        model.w -= learning_rate * d_loss * d_pred_w\n        model.b -= learning_rate * d_loss * d_pred_b\n    \n    if epoch % 20 == 0:\n        print(f"Epoch {epoch}: loss = {total_loss:.4f}")\n\nprint(f"\\nFinal predictions:")\nfor x in X:\n    print(f"  {x} -> {model.forward(x):.3f}")', hints: ['Forward, loss, backward, update', 'Manual gradients here'], solution: '# Training loop converges' },
    { title: 'Batch Processing', prompt: 'Explain why we use batches instead of processing one sample at a time.', type: 'TEXT_RESPONSE', difficulty: 'INTERMEDIATE', xpReward: 10, hints: ['GPU efficiency', 'Gradient stability'], solution: 'Batches are efficient:\n1. GPU parallelism: process many samples simultaneously\n2. Stable gradients: average over batch reduces noise\n3. Memory efficient: dont need full dataset in memory\n4. Regularization: batch noise can help generalization\n\nTypical batch sizes: 32, 64, 128, 256\nSmaller batches: more noise, might generalize better\nLarger batches: faster, more stable, might need larger LR' },
    { title: 'Learning Rate Impact', prompt: 'Demonstrate how learning rate affects training.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: 'import math\n\ndef train_with_lr(lr, epochs=50):\n    # Simple linear regression: y = wx where true w = 2\n    w = 0.0\n    X, Y = [1, 2, 3, 4], [2, 4, 6, 8]\n    \n    for epoch in range(epochs):\n        total_loss = 0\n        for x, y in zip(X, Y):\n            pred = w * x\n            loss = (pred - y) ** 2\n            total_loss += loss\n            # Gradient: d/dw (wx - y)² = 2x(wx - y)\n            grad = 2 * x * (pred - y)\n            w -= lr * grad\n        \n        if epoch == epochs - 1:\n            print(f"LR={lr}: final w={w:.4f}, loss={total_loss:.4f}")\n    return w\n\nfor lr in [0.001, 0.01, 0.1, 0.5]:\n    train_with_lr(lr)', hints: ['Too small = slow', 'Too large = unstable'], solution: '# Shows different convergence speeds' },
    { title: 'Validation Loop', prompt: 'Implement a validation loop that runs after each training epoch.', type: 'CODE', difficulty: 'INTERMEDIATE', xpReward: 20, starterCode: 'import random\nimport math\n\ndef sigmoid(x): return 1 / (1 + math.exp(-max(-500, min(500, x))))\n\n# Generate data\nrandom.seed(42)\nX_train = [[random.random(), random.random()] for _ in range(80)]\ny_train = [1 if x[0] + x[1] > 1 else 0 for x in X_train]\nX_val = [[random.random(), random.random()] for _ in range(20)]\ny_val = [1 if x[0] + x[1] > 1 else 0 for x in X_val]\n\n# Simple model\nW = [random.gauss(0, 0.1) for _ in range(2)]\nb = 0.0\nlr = 0.5\n\ndef predict(x):\n    z = sum(xi * wi for xi, wi in zip(x, W)) + b\n    return 1 if sigmoid(z) > 0.5 else 0\n\ndef accuracy(X, y):\n    correct = sum(predict(x) == yi for x, yi in zip(X, y))\n    return correct / len(y)\n\n# Training loop with validation\nfor epoch in range(20):\n    # Train\n    for x, y in zip(X_train, y_train):\n        z = sum(xi * wi for xi, wi in zip(x, W)) + b\n        pred = sigmoid(z)\n        error = pred - y\n        for i in range(2):\n            W[i] -= lr * error * pred * (1 - pred) * x[i]\n        b -= lr * error * pred * (1 - pred)\n    \n    # Validate\n    train_acc = accuracy(X_train, y_train)\n    val_acc = accuracy(X_val, y_val)\n    \n    if epoch % 5 == 0:\n        print(f"Epoch {epoch}: train_acc={train_acc:.2%}, val_acc={val_acc:.2%}")', hints: ['No gradient updates during validation', 'Compare train vs val accuracy'], solution: '# Shows train and validation accuracy' },
  ],
};

async function main() {
  console.log('Adding remaining exercises...\n');
  
  for (const [slug, exercises] of Object.entries(exercisesByLesson)) {
    const lesson = await prisma.lesson.findUnique({
      where: { slug },
      include: { exercises: true }
    });
    
    if (!lesson) {
      console.log(`⚠️  Lesson not found: ${slug}`);
      continue;
    }
    
    // For these specific lessons, add to existing
    const startNum = lesson.exercises.length + 1;
    
    let order = startNum;
    for (const ex of exercises) {
      // Check if similar title exists
      const exists = lesson.exercises.some(e => e.title === ex.title);
      if (exists) continue;
      
      await prisma.exercise.create({
        data: {
          lessonId: lesson.id,
          number: order,
          title: ex.title,
          prompt: ex.prompt,
          type: ex.type || 'CODE',
          difficulty: ex.difficulty || 'BEGINNER',
          xpReward: ex.xpReward || 10,
          starterCode: ex.starterCode || null,
          solution: ex.solution || null,
          hints: ex.hints || [],
          order: order,
        }
      });
      order++;
    }
    
    const added = order - startNum;
    if (added > 0) {
      console.log(`✓ ${slug}: added ${added} exercises (total: ${lesson.exercises.length + added})`);
    } else {
      console.log(`- ${slug}: no new exercises needed`);
    }
  }
  
  console.log('\nDone!');
}

main()
  .catch(console.error)
  .finally(() => prisma.$disconnect());
